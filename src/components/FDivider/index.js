import React from 'react';
import { Divider } from '@mui/material';

const FDivider = ({ variant, severity, text }) => {
    return (
        <Divider variant="middle" />
    )
};

export default FDivider;
